// global

//simple data
const no = false;
const numbers = 1;
const string= 'my name is inigio montoya. You killed my father. Prepare to die. ';

// 
const person = { 
    age:37,
    height: 6,
    eyeColor: 'black',
    hairColor: 'Red',
    address:{
        street: 'lives on a boat',  
        city: 'ocean',
        zip: false
    }
};
person.age='39';

const array= [ 0, 5, 'black', 'red', {
    street: 'lives on a boat',
    city: 'ocean',
    zip: false
}];

// console.log('person', person['address']['street']);
// console.log('array', array);